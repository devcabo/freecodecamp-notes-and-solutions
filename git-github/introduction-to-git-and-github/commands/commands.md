## List of commands i learned 

- `git status` shows repository changes
- `git add .` stages files for commit
- `git commit` creates a project snapshot
- `git push` uploads changes to GitHub
- `git remote -v` to see the save remote
- `git init` active the git in the folder